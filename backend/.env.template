# Plantilla de entorno del backend.
# Para Docker Compose, usá el archivo `.env` en la raíz del repositorio (ver `../.env.example`).

# Referencia Neon (mismo DSN que podés copiar del dashboard; sslmode=require):
DATABASE_URL=postgresql://NEON_USER:NEON_PASSWORD@ep-example-123456.us-east-2.aws.neon.tech/neondb?sslmode=require

DB_PROVIDER=postgres
DB_USER=NEON_USER
DB_PASS=NEON_PASSWORD
DB_HOST=ep-example-123456.us-east-2.aws.neon.tech
DB_NAME=neondb
DB_SSLMODE=require

SECRET=genera_un_secreto_largo_para_jwt

# Anthropic Claude (capa de IA ATV)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-opus-4-7
